{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "format": "prettier --write ."
    },
    "devDependencies": {
        "@prettier/plugin-php": "^0.25.0",
        "prettier": "^3.8.1"
    }
}
